@{
    ViewData["Title"] = "Potwierdzenie zakupu";
}

<div class="container">
    <div class="row justify-content-center mt-5">
        <div class="col-md-8">
            <div class="card text-center">
                <div class="card-body p-5">
                    <div class="mb-4">
                        <i class="bi bi-check-circle-fill text-success" style="font-size: 5rem;"></i>
                    </div>
                    
                    <h1 class="display-4 mb-3">Dziękujemy za zakup!</h1>
                    
                    <p class="lead mb-4">
                        Twoje zamówienie zostało zrealizowane pomyślnie.
                    </p>

                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success">
                            @TempData["SuccessMessage"]
                        </div>
                    }

                    <div class="alert alert-info">
                        <i class="bi bi-info-circle"></i> 
                        Zakupione gry są już dostępne w Twojej bibliotece!
                    </div>

                    <div class="d-grid gap-2 d-md-block">
                        <a asp-controller="Library" asp-action="Index" class="btn btn-primary btn-lg me-2">
                            <i class="bi bi-collection"></i> Przejdź do biblioteki
                        </a>
                        <a asp-controller="Library" asp-action="Purchases" class="btn btn-outline-primary btn-lg me-2">
                            <i class="bi bi-receipt"></i> Historia zakupów
                        </a>
                        <a asp-controller="Games" asp-action="Index" class="btn btn-outline-secondary btn-lg">
                            <i class="bi bi-shop"></i> Kontynuuj zakupy
                        </a>
                    </div>
                </div>
            </div>

            <div class="card mt-4">
                <div class="card-body">
                    <h5><i class="bi bi-lightbulb"></i> Co dalej?</h5>
                    <ul>
                        <li>Zakupione gry znajdują się w Twojej bibliotece</li>
                        <li>Możesz je pobrać i zainstalować w dowolnym momencie</li>
                        <li>Otrzymasz email z potwierdzeniem zakupu</li>
                        <li>W razie pytań skontaktuj się z naszym wsparciem</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">